import { IconName } from '@appsemble/sdk';

export interface Button {
  /**
   * A [Font Awesome icon](https://fontawesome.com/icons?m=free) name to render on the button.
   *
   * By default a cared pointing left or right